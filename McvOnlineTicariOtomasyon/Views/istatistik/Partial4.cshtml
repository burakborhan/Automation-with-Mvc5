@model IEnumerable<McvOnlineTicariOtomasyon.Models.siniflar.SinifGrup3>
<div class="card">
    <div class="card-header">
        <h3 class="card-title">Marka Full Width Table</h3>
    </div>
    <!-- /.card-header -->
    <div class="card-body p-0">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th style="width: 10px">#</th>
                    <th>Marka</th>
                    <th>İlerleme Barı</th>
                    <th style="width: 40px">Sayı</th>
                </tr>
            </thead>
            <tbody>
                @{int sirano = 0; }
                @foreach (var x in Model)
                {
                    var a = x.sayi * 10;
                    <tr>
                        <td>@{sirano++;} @sirano</td>
                        <td>@x.marka</td>
                        <td>
                            <div class="progress progress-xs">
                                <div class="progress-bar progress-bar-danger" style="width: @a%"></div>
                            </div>
                        </td>
                        <td><span class="badge bg-danger">@x.sayi</span></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <!-- /.card-body -->
</div>
